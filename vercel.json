{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    },
    {
      "src": "apps/api/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/v1/(.*)", "dest": "apps/api/api/index.ts" },
    { "src": "/(.*)", "dest": "apps/web/dist/$1" }
  ]
}
